The GRUB package is used as a loader of the Linux system.

The Preupgrade Assistant will back up the /boot/grub/splash.xpm.gz file 
and restore it after the upgrade to the /boot/grub directory.

The file splash.xpm.gz allows you to modify boot parameters during the booting.
