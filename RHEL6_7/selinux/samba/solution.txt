We found some samba shared directories. These should be labeled as samba_shared_t, unless they are system directories, which should carry a different label. Since autorelabel will be performed after the upgrade, check if they are labeled correctly on the upgraded system.
